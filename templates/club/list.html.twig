<img src="{{ asset('img/islem.jpg') }}" alt="student "
width="100px " height="100px">
<h1>list des formations</h1>
de la classe {{ x }} , dans la salle {{ y }}

{% if tabFormation is empty %}
<h1>Pas de formation</h1>
{% else %}
<table border="1">
     <tr>
        <td>ref</td>
        <td>Titre</td>
        <td>Description</td>
        <td>nb_participants</td>
         <td>Participant</td>
    </tr>
   {% for i in tabFormation %}
       {% if i.Description == "formation theorique" %}
            <tr>
        <td>{{ i.ref }}</td>
        <td>{{ i.Titre|upper }}</td>
        <td>{{ i.Description }}</td>
        <td>{{ i.nb_participants }}</td>
       <td>
           {% if i. nb_participants ==0 %}
               <a href="{{ path('formation') }}">Participer</a>
           {% else %}
               solde out
           {% endif %}
       </td>
    </tr>
{% else %}
       {% endif%}
{% endfor  %}


</table>
{% endif %}
  Nombre de formations : {{ tabFormation|length }} <br>
 Le titre de la formation ayant le plus grand nombre de participants : {{ maxTitre}}    <br>
 Le nombre total de participants dans toutes les formations :
 {% set nb_participants = 0 %}
 {% for t in tabFormation %}
     {% set nb_participants = nb_participants+ t.nb_participants %}
 {% endfor %}

{{nb_participants}}
